experiment 4 
#include <stdio.h>

int main() {
    int n, i, j, temp;
    float avg_wait = 0, avg_tat = 0;

    printf("Enter number of processes: ");
    scanf("%d", &n);

    int bt[n], pid[n], wt[n], tat[n];

    // Input burst time for each process and assign process id
    for (i = 0; i < n; i++) {
        printf("Enter burst time for process %d: ", i + 1);
        scanf("%d", &bt[i]);
        pid[i] = i + 1;
    }

    // Sort processes by burst time
    for (i = 0; i < n - 1; i++) {
        for (j = i + 1; j < n; j++) {
            if (bt[i] > bt[j]) {
                // Swap burst times
                temp = bt[i];
                bt[i] = bt[j];
                bt[j] = temp;
                // Swap process IDs
                temp = pid[i];
                pid[i] = pid[j];
                pid[j] = temp;
            }
        }
    }

    wt[0] = 0;  // Waiting time for first process is 0

    // Calculate waiting time
    for (i = 1; i < n; i++) {
        wt[i] = wt[i - 1] + bt[i - 1];
    }

    // Calculate turnaround time
    for (i = 0; i < n; i++) {
        tat[i] = wt[i] + bt[i];
    }

    // Calculate total waiting time and turnaround time
    for (i = 0; i < n; i++) {
        avg_wait += wt[i];
        avg_tat += tat[i];
    }

    avg_wait /= n;
    avg_tat /= n;

    // Display results
    printf("
Process\tBurst Time\tWaiting Time\tTurnaround Time
");
    for (i = 0; i < n; i++) {
        printf("P%d\t%d\t\t%d\t\t%d
", pid[i], bt[i], wt[i], tat[i]);
    }

    printf("
Average Waiting Time: %.2f", avg_wait);
    printf("
Average Turnaround Time: %.2f
", avg_tat);

    return 0;
}


output:
Enter burst time for process 6: 83
Process	Burst Time	Waiting Time	Turnaround TimeP2	11		0		11P5	12		11		23P1	20		23		43P3	34		43		77P4	55		77		132P6	83		132		215Average Waiting Time: 47.67Average Turnaround Time: 83.50